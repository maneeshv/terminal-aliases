user prefers absolute imports instead of relative imports
user prefers to use context7 mcp to get documentation on the libraries to prevent hallunications
user prefers to use UV for managing packages
